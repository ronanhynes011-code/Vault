# Vault Executor

WPF Roblox executor. Obsidian + gold theme. Native API backend via Vault.dll.

## Build

Pushed to main → GitHub Actions builds Vault.exe → download from Actions artifacts.

## Layout

- `native/` — put Vault.dll and its deps here before pushing
- `.github/workflows/build.yml` — the build pipeline